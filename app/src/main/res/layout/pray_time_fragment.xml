<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layoutDirection="ltr"
        >
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/praiesTimes"
            android:orientation="horizontal"
            android:background="@drawable/pray_tims_title_bg"
            android:id="@+id/layout"
            >
            <TextView
                style="@style/textStyle"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:paddingVertical="15dp"
                android:id="@+id/title"
                android:text="@string/prayTims"
                android:textSize="25sp"
                android:textStyle="bold"
                android:textColor="@color/prayTimsTitle"
                android:gravity="center"
                />
        </LinearLayout>
        <androidx.recyclerview.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@+id/layout"
            android:id="@+id/praiesTimes"
            android:background="@color/prayRecycleBG" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>